import 'package:flutter/painting.dart';

const double baseEdgeInsets = 8;

const EdgeInsets paddingAllSmall = EdgeInsets.all(baseEdgeInsets * 1);

const EdgeInsets paddingAllMedium = EdgeInsets.all(baseEdgeInsets * 2);

const EdgeInsets paddingAllLarge = EdgeInsets.all(baseEdgeInsets * 3);

const EdgeInsets paddingTopSmall = EdgeInsets.only(top: baseEdgeInsets * 1);

const EdgeInsets paddingTopMedium = EdgeInsets.only(top: baseEdgeInsets * 2);

const EdgeInsets paddingTopLarge = EdgeInsets.only(top: baseEdgeInsets * 3);

const EdgeInsets paddingRightSmall = EdgeInsets.only(right: baseEdgeInsets * 1);

const EdgeInsets paddingRightMedium = EdgeInsets.only(right: baseEdgeInsets * 2);

const EdgeInsets paddingRightLarge = EdgeInsets.only(right: baseEdgeInsets * 3);

const EdgeInsets paddingLeftSmall = EdgeInsets.only(left: baseEdgeInsets * 1);

const EdgeInsets paddingLeftMedium = EdgeInsets.only(left: baseEdgeInsets * 2);

const EdgeInsets paddingLeftLarge = EdgeInsets.only(left: baseEdgeInsets * 3);

const EdgeInsets paddingBottomSmall = EdgeInsets.only(bottom: baseEdgeInsets * 1);

const EdgeInsets paddingBottomMedium = EdgeInsets.only(bottom: baseEdgeInsets * 2);

const EdgeInsets paddingBottomLarge = EdgeInsets.only(bottom: baseEdgeInsets * 3);

const EdgeInsets paddingHorizontalSmall = EdgeInsets.symmetric(horizontal: baseEdgeInsets * 1);

const EdgeInsets paddingHorizontalMedium = EdgeInsets.symmetric(horizontal: baseEdgeInsets * 2);

const EdgeInsets paddingHorizontalLarge = EdgeInsets.symmetric(horizontal: baseEdgeInsets * 3);

const EdgeInsets paddingVerticalSmall = EdgeInsets.symmetric(vertical: baseEdgeInsets * 1);

const EdgeInsets paddingVerticalMedium = EdgeInsets.symmetric(vertical: baseEdgeInsets * 2);

const EdgeInsets paddingVerticalLarge = EdgeInsets.symmetric(vertical: baseEdgeInsets * 3);

const EdgeInsets marginAllSmall = EdgeInsets.all(baseEdgeInsets * 1);

const EdgeInsets marginAllMedium = EdgeInsets.all(baseEdgeInsets * 2);

const EdgeInsets marginAllLarge = EdgeInsets.all(baseEdgeInsets * 3);

const EdgeInsets marginTopSmall = EdgeInsets.only(top: baseEdgeInsets * 1);

const EdgeInsets marginTopMedium = EdgeInsets.only(top: baseEdgeInsets * 2);

const EdgeInsets marginTopLarge = EdgeInsets.only(top: baseEdgeInsets * 3);

const EdgeInsets marginBottomSmall = EdgeInsets.only(bottom: baseEdgeInsets * 1);

const EdgeInsets marginBottomMedium = EdgeInsets.only(bottom: baseEdgeInsets * 2);

const EdgeInsets marginBottomLarge = EdgeInsets.only(bottom: baseEdgeInsets * 3);

const EdgeInsets marginHorizontalSmall = EdgeInsets.symmetric(horizontal: baseEdgeInsets * 1);

const EdgeInsets marginHorizontalMedium = EdgeInsets.symmetric(horizontal: baseEdgeInsets * 2);

const EdgeInsets marginHorizontalLarge = EdgeInsets.symmetric(horizontal: baseEdgeInsets * 3);

const EdgeInsets marginVerticalSmall = EdgeInsets.symmetric(vertical: baseEdgeInsets * 1);

const EdgeInsets marginVerticalMedium = EdgeInsets.symmetric(vertical: baseEdgeInsets * 2);

const EdgeInsets marginVerticalLarge = EdgeInsets.symmetric(vertical: baseEdgeInsets * 3);


